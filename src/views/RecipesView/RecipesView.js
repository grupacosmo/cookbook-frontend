import React from 'react';

const RecipesView = () => {
    /* todo(filip.bulanda)
        Create simple layout with list of recipes (title, button to open recipe, delete button.)
        Every recipe should have it's own id. (Just create dummy hardcoded list of recipes)
        Button to open recipe should redirect user to /recipe/{recipe_id}
        Delete button should remove recipe from list
     */
    return (
        <img style={{width: "100%"}} src="https://a.allegroimg.com/s1024/0cb18c/686290334c29a21d2ca391ee12bd" />
    )
};

export default RecipesView;
