import React from 'react';

const LoginView = () => {
    /* todo(natalia.burdzy)
        Create simple layout with login form.
        Add link to '/register' url with text 'Don't have account? Click here!'
        Use validation 'https://ant.design/components/form/#components-form-demo-validate-static'. E.g. if fields are empty.
        After successful login 'let's say, every login and password is ok', redirect user to '/recipes' url.
     */
    return (
        <img style={{width: "100%"}} src="http://i.imgur.com/rcCFGSf.png" />
    )
};

export default LoginView;
