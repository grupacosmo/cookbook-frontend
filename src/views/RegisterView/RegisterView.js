import React from 'react';

const RegisterView = () => {
    /* todo(dawid.laska)
        Create simple layout with register form (username, password, confirm password etc)
        Use validation 'https://ant.design/components/form/#components-form-demo-validate-static'. E.g. if fields are empty. Or passwords are not matching
        After successful register 'let's say, every try is successful', redirect user to '/login url.
     */
    return (
        <img style={{width: "100%"}} src="https://www.metin2pserver.info/screens/Bobi2.png" />
    )
};

export default RegisterView;
